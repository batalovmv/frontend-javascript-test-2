name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Деплой при пуше в main

permissions:
  contents: write  # Разрешаем Actions пушить файлы

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Checkout репозитория
        uses: actions/checkout@v3

      - name: 🛠️ Установка Docker
        uses: docker/setup-buildx-action@v2

      - name: 🏗️ Сборка Docker-образа
        run: docker build -t my-vite-app .

      - name: 📦 Извлечение `dist/` из контейнера
        run: |
          CONTAINER_ID=$(docker create my-vite-app)  # Запускаем контейнер
          docker cp $CONTAINER_ID:/app/react-app/dist ./dist  # Копируем dist/
          docker rm -v $CONTAINER_ID  # Удаляем контейнер

      - name: 🚀 Деплой на GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages  # Ветка для GitHub Pages
          folder: dist  # Папка с готовыми файлами